export type User = {
    id: string;
    name: string;
    image: string;
    staff: boolean;
    globalMod: boolean;
    owner: boolean;
    advisor: boolean;
    council: boolean;
    observer: boolean;
};
